// TODO: Replace with the real dashboard (session list, timeline, event
// inspector) once the capture pipeline and server routes exist.
function App() {
  return <p>Wevna Dashboard</p>;
}

export default App;
