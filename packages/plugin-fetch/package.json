{
  "name": "@wevna/plugin-fetch",
  "version": "0.0.1",
  "private": true,
  "description": "Wevna plugin capturing outgoing HTTP requests made with global fetch.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup && tsc --build",
    "dev": "tsup --watch",
    "check": "tsc --build",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "peerDependencies": {
    "wevna": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wevna": "workspace:*"
  }
}
