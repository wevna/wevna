// TODO: Replace this placeholder with the real SDK entry point once runtime
// capture (AsyncLocalStorage hooks, EventEmitter patching, transport to the
// local server) is implemented.
export const wevna = {
  start() {
    console.log("Wevna SDK placeholder");
  },
};
